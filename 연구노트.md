## 환경 / 의존성
- Python 3.10–3.12(`requires-python >=3.10,<3.13`)만 공식 범위로 정리함.
- Torch/PyG를 `dependency-groups`의 `cpu`와 `xpu`로 분리하고 `tool.uv.conflicts`로 배제함.
- 기본 `uv sync`는 `default-groups = ["cpu"]`로 CPU PyTorch 2.2 스택 유지.
- XPU는 `torch 2.9.1+xpu` + `pyg`는 `2.9.1+cpu` 휠 페이지 + `pytorch-triton-xpu`를 `whl/xpu`에서 끌도록 설정함.
- `uv.lock`을 다시 잡아 Intel 런타임·트리톤 등이 XPU 포크에 들어가게 함.
- README에 XPU/CPU 전환용 `uv sync` 명령을 적어 둠.
- `pick_torch_device` 설명을 `torch+xpu` 우선으로 수정함.
- 작업 머신: CPU 8코어 + Intel Arc 140V (XPU, 16 GB). `torch 2.9.1+xpu` 정상 인식.

## QDF Rust 포팅 — 방향성
- 작업 범위는 `QuantumDeepField_molecule/` 만. 그 외 폴더는 손대지 않음.
- 핵심 전제: **무거운 CPU 영역만 Rust로 다시 짠다.** XPU 위 GEMM/오토그래드는 PyTorch+IPEX에 그대로 맡긴다.
- 통합 방식: PyO3 (가장 흔한 방식). Rust 크레이트를 `maturin`으로 빌드해 Python에서 import.
- GPU(Rust) 옵션은 폐기 — Intel XPU 위에서 학습 그래프를 Rust로 다시 짤 가치가 없음. (`wgpu`/OpenCL/Level Zero 직접 호출은 autograd·optimizer까지 직접 만들어야 해서 학술 코드에서 손해.)

## 프로파일링 셋업
- `QuantumDeepField_molecule/bench/profile_preprocess.py`
  - `train/preprocess.py`의 함수(`create_sphere`, `create_field`, `create_distancematrix`, `create_potential`)를 import 해서 호출.
  - 분자 단위 루프를 다시 돌리면서 단계별 wall time / cProfile 수집.
  - 옵션: `--limit`, `--no-save`, `--cprofile`.
- `QuantumDeepField_molecule/bench/profile_train.py`
  - `train/train.py`의 `MyDataset` / `mydataloader` / `QuantumDeepField` 재사용.
  - phase A: 데이터로더만 순회. phase B: 학습 한 epoch을 `fwd_E/bwd_E/step_E/fwd_V/bwd_V/step_V/io_next_batch`로 분해.
  - XPU 측정은 매 단계 `torch.xpu.synchronize()` 호출, `--warmup-batches`로 첫 배치(JIT 워밍) 제외.

## 1차 측정 결과
### preprocess (`QM9under14atoms` train 500분자, 단일 스레드)
- wall 8.73 s → 17.5 ms/molecule. **QM9full(130k) 외삽 약 38분.**
- 단계별 비중
  - `distance_matrix(field, orbital_coords)` 57.1 %
  - `np.save` (디스크 IO, 분자당 .npy 1개) 24.2 %
  - `distance_matrix(field, atom_coords)` 6.1 %
  - `create_field` (Python list comprehension) 5.6 %
  - 나머지(파싱·potential 등) 합쳐 ~7 %
- cProfile inner cost: `scipy._kdtree.minkowski_distance_p` 가 단일 self-time 1위. 거리행렬 계산이 진짜 hot spot.
- **결론**: 거리행렬 + 격자 생성 + 디스크 IO 합쳐서 ≈ 93 %. Rust + Rayon으로 분자 단위 병렬화 + 단일 binary shard 저장이면 큰 차이가 날 가능성 높음.

### training (`QM9under7atoms_homolumo_eV`, batch 8, 3 epoch, Intel Arc 140V XPU)
- 너무 작은 데이터셋(train 34개, batch 5개)이라 절대 시간보다는 비율 위주로 해석.
- phase A (모델 없이 데이터로더 순회): 260 ms/batch — cold cache 기준 IO 영향.
- phase B (warmup 제외 평균, ms/batch):
  - `fwd_E` 98.8, `bwd_E` 222.2, `step_E` 5.1
  - `fwd_V` 60.9, `bwd_V` 102.9, `step_V` 4.8
  - `io_next_batch` 6.5 (warm cache)
- 비중: fwd+bwd 합쳐 96 %. step(optimizer) 2 %, IO 1.3 %.
- 주의: `model.LCAO`/`model.pad`/`list_to_batch` 의 CPU→XPU 복사 비용이 현재는 `fwd_*` 안에 묻혀 있음. 큰 분자 데이터셋에서 다시 검증 필요.
- 잠정 결론: 작은 분자 영역에서는 GPU 연산이 지배적이라 Rust로 옮길 여지 적음. 다만 `pad()`(블록 대각 빌더)는 분자 크기 제곱으로 커지므로 큰 분자에선 별도 측정 필요.

## 우선순위 (Rust 포팅 후보)
1. **전처리 파이프라인 통째**: `create_field` + `create_distancematrix` + `create_potential` + 저장 포맷.
   - Rayon으로 분자 단위 데이터 병렬, `ndarray` + 직접 거리 계산(scipy 콜백 오버헤드 제거).
   - 저장은 분자 130k개 .npy → 단일 binary shard(예: little-endian raw, 헤더 = 오프셋 테이블) 또는 `arrow`/`safetensors` 검토.
   - PyO3로 두 가지 인터페이스 제공: (a) CLI 같은 일괄 변환, (b) Python에서 `Dataset`이 mmap으로 직접 읽도록 하는 헬퍼.
2. **Dataset 로더**: 1번 결과 포맷을 mmap으로 읽고 batch 단위로 묶어 PyTorch tensor로 zero-copy(가능하면) 반환.
3. **`model.pad` 블록 대각 빌더**(보류): 큰 분자 셋(`QM9under14atoms` 이상)을 전처리한 뒤 재측정해서 비중 확인 후 결정.

## 다음 할 일
- `QM9under14atoms_atomizationenergy_eV` 를 한 번 전처리해서 큰 분자 기준으로 training 프로파일 다시 측정 (현재 결과는 너무 작은 분자라 일반화 어려움).
- 위 1번을 위한 Rust 크레이트 스켈레톤 (`qdf_preprocess/`)을 `maturin` + PyO3로 만들기 시작.
- 측정 결과는 `bench/` 폴더에 텍스트로도 보관 (재현용).

## 작업 규칙 (사용자 합의)
- **기존 구현을 절대 삭제하지 않는다.** Rust 구현을 도입할 때도 기존 NumPy/PyTorch 경로를 옆에 그대로 둔다.
- 새 경로는 선택 가능해야 한다. 예시:
  - `preprocess.py` → `--backend {numpy,rust}` 인자 추가 (기본 `numpy`)
  - 데이터 로더 → `MyDataset` 옆에 `MyDatasetShard`(Rust 백엔드용) 클래스를 추가. 사용 측 코드(`train.py`)는 한 줄로 스위칭.
  - `train.py` 모델 → `model.pad`/`model.list_to_batch`를 모듈화하고 `--pad-impl {python,rust}` 인자 도입.
- 모든 새 인자는 기본값을 기존 동작과 동일하게 둔다 → 기존 사용자 워크플로우가 깨지지 않게.

## 2차 측정 (큰 분자 기준, `QM9under14atoms`)
### 전처리 자체 (사용한 건 기존 numpy 경로)
- 전체 `QM9under14atoms` (train 11832 + val 1480 + test 1479 = 14791개) 전처리 wall time **약 305 초** (단일 스레드).
- 분자당 평균 ≈ 20.6 ms. QM9full 외삽 ≈ 45 분. 앞선 500-분자 측정(38분 예상)과 일관.

### `profile_train.py` (Intel Arc 140V XPU, batch 8, sum operation, 60 batches × 2 epoch, warmup 2)
- phase A (모델 없이 데이터로더만): **226.5 ms/batch, 28.3 ms/sample**. 분자당 .npy 1개 읽고 `np.load(allow_pickle=True)` 하는 비용 그대로 보임.
- phase B (warmup 제외 합계 116 배치):
  ```
  io_next_batch   33.3 %   244.7 ms/batch   ← 1위
  bwd_E           23.9 %   176.1 ms/batch
  bwd_V           18.6 %   136.5 ms/batch
  fwd_E           11.9 %    87.6 ms/batch
  fwd_V           11.0 %    81.1 ms/batch
  step_E + step_V  1.3 %     9.6 ms/batch
  ```
- LCAO 분해 (240 fwd 호출, sync 후 측정):
  ```
  pad             19.6 ms/call   (CPU 블록 대각 빌더)
  list_to_batch    4.6 ms/call × 4 호출/fwd ≈ 18.4 ms
  basis_matrix    14.8 ms/call   (XPU)
  functional      11.1 ms/call   (XPU MLP)
  HKmap            6.1 ms/call   (XPU MLP)
  ```
- 해석:
  - fwd ~85 ms 중 **약 38 ms (45 %)** 가 CPU 측 패딩 + host→device 복사. 절반 가까이는 GPU 가속 영역 밖.
  - bwd 가 fwd 대비 2x 가까운데, `pad`로 만든 큰 block-diagonal 텐서를 따라 흐르는 그래프 비용이 일부 들어가 있을 가능성. 패딩 자체가 학습 그래프 안에 있어서 backward도 영향을 받음.

## 우선순위 (갱신)
1. **데이터 IO 파이프라인** (전체의 33 %)
   - 130k개 `.npy` → 단일 shard 포맷(헤더 + 오프셋 테이블 + raw float32). Rust로 작성, mmap 기반 zero-copy 로더.
   - Python 측은 `MyDatasetShard` 신규 클래스로 노출. 기존 `MyDataset`은 손대지 않음.
2. **`pad()` 블록 대각 빌더 + `list_to_batch`** (fwd 안의 ~45 %, 전체의 ~10 %)
   - 한 가지 옵션: 전처리 시점에 이미 분자별로 padded 텐서를 만들어 단일 shard에 저장 → 학습 때는 그냥 인덱싱.
   - 다른 옵션: `pad` 만 Rust(PyO3)로 옮겨 사전 인덱스 계산.
   - `model.pad`/`model.list_to_batch`는 `--pad-impl` 스위치로 갈아낄 수 있게 하고, 기존 Python 구현 유지.
3. **전처리 본체** (시급도 ↓, 한 번 돌리고 끝)
   - 단일 분자 38 분 → Rust + Rayon으로 5 분 이하 기대. 1번 작업과 함께 같은 크레이트(`qdf_preprocess`)에서 처리 가능.

## bench 스크립트 보강 (이번 작업)
- `bench/profile_train.py`에 `--max-batches`, `--lcao-breakdown` 추가.
- `--lcao-breakdown`은 `model.list_to_batch / pad / basis_matrix / functional / HKmap`을 timing wrapper로 감싸 호출 단위 평균을 출력. 기존 모델 코드는 수정하지 않음 (런타임 monkey-patch).

## Rust IO 파이프라인 1차 구현 (작업 완료)
### 결과물
- `QuantumDeepField_molecule/qdf_io/` — PyO3 + maturin 기반 Rust 크레이트.
  - `Cargo.toml`, `pyproject.toml`(maturin), `src/lib.rs`, `python/qdf_io/__init__.py`.
  - 의존성: `pyo3 = 0.22`, `numpy = 0.22`, `ndarray = 0.16`, `memmap2 = 0.9`, `rayon = 1.12`.
  - 빌드: `.\.venv\Scripts\maturin.exe develop --release` (qdf_io 폴더에서).
  - 노출(요약):
    - IO: `ShardReader`, `format_info()`
    - LCAO 호스트 헬퍼: `block_diag_pad_f32`, `concat_i64`, `concat_f32_axis0`, `concat_f32_axis1`
    - 전처리: `preprocess_molecule_rust`, `preprocess_batch_rust` (Rayon, GIL 해제)
- `QuantumDeepField_molecule/train/dataset_shard.py` — 같은 포맷의 Python 라이터(`write_shard`) + 데이터셋 래퍼(`MyDatasetShard`). 파일 상단에 포맷 사양을 주석으로 박아둠.
- `QuantumDeepField_molecule/bench/convert_to_shard.py` — `.npy/` → `*_shard.bin` 변환 CLI.
- `QuantumDeepField_molecule/bench/verify_shard.py` — 같은 분자를 `MyDataset`(npy)과 `MyDatasetShard`로 읽어 비트 단위 비교.
- `profile_train.py`에 `--loader {npy,shard}` 인자(기본 `npy`). 기존 `MyDataset` 경로는 그대로 두고 분기만 추가.
- `profile_train.py`에 `--pad-impl {python,rust,rust-pad-only}` 인자(기본 `python`). `train/model_patches.py`가 인스턴스 메서드만 monkey-patch 하며 **원본 클래스 정의는 건드리지 않음**.
- `bench/verify_lcao_patch.py` — 동일 배치에 대해 python vs rust 패치의 loss(E/V)가 비트 단위로 동일한지 검증.

### Shard 포맷 v1 (요약)
- LE 정렬 64B 헤더(magic = `QDFSHRD\0`, version=1, n_molecules, n_output, flags, index_table_offset, data_section_offset, file_size).
- 분자 인덱스 테이블 = `n_molecules * u64`.
- 분자 레코드 = `n_orbitals/n_field/idx_len/reserved (u32×4) + idx_bytes (+ pad8) + int64[n_orb] + f32[N_field, N_orb] + f32[N_orb] + f32 + (옵션) f32[n_output] + f32[N_field] (+ pad8)`.
- 모든 분자 레코드 시작은 8B 정렬, 디스크 → mmap → `std::ptr::copy_nonoverlapping` 으로 새 `Vec`에 복사 후 `ndarray::Array2 → into_pyarray_bound` 로 numpy ndarray 반환.
- 반환 튜플은 `MyDataset.__getitem__`이 주는 6-/8-튜플과 dtype·shape가 정확히 동일.

### 변환 결과 (QM9under14atoms, 단일 스레드 Python 라이터)
| split | molecules | size | wall |
|---|---|---|---|
| train | 11832 | 4.53 GiB | ~280 s |
| val   | 1479  | 0.55 GiB | 29.2 s |
| test  | 1480  | 0.56 GiB | 28.3 s |

분자 1개당 약 19~20 ms (대부분 `np.load(allow_pickle=True)` + 디스크 쓰기). 한 번만 돌리는 작업이라 추후 우선순위는 낮음.

### 정확성 검증
- `verify_shard.py` (val 20개 + train 30개 샘플)에서 모든 분자의 8개 필드가 `np.array_equal` 통과.

### npy vs shard 벤치마크 (Intel Arc 140V XPU, batch_size 8, 60 batches × 2 epoch, warmup 2, **OS 캐시 warm**)
| phase | npy run1 | npy run2 | shard run1 | shard run2 |
|---|---|---|---|---|
| Phase A (dataloader only) | 30.4 ms/batch | — | 8.6 ms/batch | — |
| Phase B total counted | 85.3 s | 81.6 s | 56.1 s | 44.7 s |
| io_next_batch | 28.1 ms | 26.4 ms | 16.4 ms | 21.0 ms |
| fwd_E | 117.5 ms | 110.4 ms | 79.4 ms | 62.6 ms |
| bwd_E | 224.4 ms | 239.7 ms | 149.5 ms | 119.3 ms |
| fwd_V | 116.6 ms | 97.7 ms | 75.0 ms | 58.0 ms |
| bwd_V | 235.5 ms | 217.1 ms | 152.4 ms | 116.1 ms |
| pad | 23.1 ms | 18.7 ms | 17.1 ms | 13.9 ms |
| list_to_batch | 5.5 ms | 4.9 ms | 4.1 ms | 3.0 ms |
| basis_matrix | 20.2 ms | 20.0 ms | 14.6 ms | 13.6 ms |

- Phase A (순수 데이터 로딩): **30.4 ms → 8.6 ms (3.5x)**.
- Phase B 전체 wall: **약 1.5~1.8x 빠름**. 단지 IO뿐 아니라 fwd/bwd 시간도 줄어든다.
- 가설: `np.load(allow_pickle=True)`가 만드는 numpy 배열은 pickle 디시리얼라이저가 임시 객체를 잔뜩 만들고 메모리 단편화도 일으킨다. shard 쪽은 깔끔한 `memcpy → Vec → into_pyarray_bound` 로 만든 연속 버퍼라 이후 `torch.FloatTensor(...).to(device)`까지의 호스트 작업이 더 빠르고 CPU↔GPU 큐 운영도 매끄러워지는 듯.

### 캐시 상태에 따른 IO 비용 (참고)
- 최초 측정(이전 섹션)에서 npy의 `io_next_batch` 244 ms/batch였던 것은 **cold cache** 상태(전처리 직후, IPEX 학습이 처음 .npy를 읽음).
- 이후 변환 작업으로 OS 캐시가 따뜻해진 상태에서 npy의 IO는 28 ms/batch까지 떨어짐.
- shard 쪽 mmap은 cold 상태에서도 큰 파일을 sequential read 하므로 격차가 더 벌어질 것으로 예상. 추후 cold cache 비교는 별도 작업(파일 캐시 비우는 도구 필요).

### 사용 방법 요약
```powershell
# 1. (한 번) Rust 확장 빌드
cd QuantumDeepField_molecule\qdf_io
C:\DGCL\3DGCL\.venv\Scripts\maturin.exe develop --release
cd ..\..

# 2. (데이터셋마다 한 번) .npy → shard 변환
C:\DGCL\3DGCL\.venv\Scripts\python.exe QuantumDeepField_molecule\bench\convert_to_shard.py `
  --dataset QM9under14atoms_atomizationenergy_eV `
  --basis-set 6-31G --radius 0.75 --grid-interval 0.3

# 3. 비교 측정
C:\DGCL\3DGCL\.venv\Scripts\python.exe QuantumDeepField_molecule\bench\profile_train.py `
  --dataset QM9under14atoms_atomizationenergy_eV `
  --operation sum --batch-size 8 --epochs 2 --max-batches 60 --warmup-batches 2 `
  --lcao-breakdown --loader npy        # 기존
C:\DGCL\3DGCL\.venv\Scripts\python.exe QuantumDeepField_molecule\bench\profile_train.py `
  ... --loader shard                   # Rust mmap
```

## LCAO `pad` / `list_to_batch` Rust 실험 (완료, 결론: 현재 환경에선 비추천)
- 구현: `qdf_io`에 CPU-side 블록 대각/concat 커널 추가 + `train/model_patches.py`에서 인스턴스 메서드만 교체.
- 정확성: `bench/verify_lcao_patch.py`에서 **loss(E/V)가 python과 완전히 동일**(abs diff 0) 확인.
- 속도(예: `QM9under14atoms`, `batch_size=8`, `loader=shard`, warm cache, 60×2 batches, `--lcao-breakdown`):
  - `--pad-impl python`: 전체 counted wall **~47 s**, `pad` **~14 ms/call**, `list_to_batch` **~4.4 ms/call**.
  - `--pad-impl rust-pad-only`: 전체 **~48 s**, `pad` **~29 ms/call** (오히려 느림).
  - `--pad-impl rust` (pad+list_to_batch): 전체 **~72 s**, `list_to_batch` **~1.1 ms/call**로 줄지만 `pad`가 여전히 느리고 `bwd_*`가 커져 **총합은 악화**.
- 해석(가설): 원본은 작은 host→device 전송이 여러 번 겹쳐 XPU 큐에서 비동기로 숨는 반면, 큰 단일 `torch.from_numpy(...).to(xpu)`는 동기화/대기 비용이 커질 수 있음. 또한 backward 그래프 형태가 달라져 `bwd_*`가 커질 여지.
- 정책: 코드/스위치는 남겨두되, **기본값은 python 유지**. 필요 시 다른 GPU/배치 크기에서 재평가.

## preprocess Rust+Rayon (완료)
### 구현
- `train/preprocess.py`에 `--backend {numpy,rust}` + `--rust-batch-size`(기본 64) 추가. 기본은 **numpy(기존 SciPy 경로)**.
- `--output-format {npy,shard,both}`: shard/`both`일 때 전처리 루프에서 `qdf_io.ShardWriter`로 `train_<field>_shard.bin` 등을 직접 기록(`.npy` 130k개 생략 가능).
- 무거운 기하 연산은 `qdf_io.preprocess_batch_rust`로 이동(Rayon 병렬, `py.allow_threads`로 GIL 해제).
- `predict/preprocess.py`도 동일 인자를 `pp.create_dataset(...)`로 전달.
- `train/preprocess.sh`, `predict/preprocess.sh`는 `PREPROCESS_BACKEND` / `PREPROCESS_RUST_BATCH_SIZE` / `PREPROCESS_OUTPUT_FORMAT` 환경변수로 선택 가능(기본 numpy / npy).
- 정확성: `bench/verify_preprocess_backend.py`로 `QM9under14atoms/train` **200분자**에서 numpy vs rust가 `rtol=1e-5, atol=1e-5` 내 일치.

### 벤치 (`bench/profile_preprocess.py`, train 1000분자, `--no-save`, 스트리밍 파싱)
| backend | wall | 비고 |
|---|---:|---|
| numpy | **8.734 s** (8.73 ms/mol) | 기존 SciPy `distance_matrix` 병목 그대로 |
| rust (`--rust-batch-size 64`) | **0.390 s** (0.39 ms/mol) | **~22.4x** (8코어 머신, Rayon 병렬) |

- rust wall의 상당 부분은 여전히 Python 파싱(`parse_molecule` ~0.15 s). 그래도 기하 연산 자체는 `rust_preprocess_batch` ~0.18 s 수준으로 급감.
- `train/preprocess.py`는 **QM9full 같은 대형 셋에서도 메모리 폭주가 나지 않게** 분자를 한 줄씩 파싱하고 rust 배치만 버퍼링(`rust_batch_size`)한다.
- `np.save`까지 포함한 end-to-end는 디스크/파일시스템에 좌우되므로, 위 표는 **compute-only** 비교.

## 비교 노트북 + 자원 측정 (완료)
### 측정 모듈 `bench/sampler.py`
- `run_with_metrics(cmd, ...)` 한 함수로 wall time + RSS + CPU% + XPU% 동시 수집. 기본 sampling tick = **100 ms (10 Hz)**.
- **CPU% (Windows)**: PDH 멀티 인스턴스 카운터 `\Process V2(*)\% Processor Time` 를 ctypes 로 직접 호출. 인스턴스 이름의 `:PID` 를 파싱해 측정 대상 PID만 합산. **0–100% (system fraction)** 컨벤션: 100% = 모든 논리 코어 풀가동.
  - `psutil.Process.cpu_percent` 는 Windows 의 `GetProcessTimes` lazy accounting 때문에 CPU-바운드 자식 프로세스에 대해 0% 만 반환하는 버그가 있어 사용 불가. 비-Windows 환경에서만 폴백으로 사용하며, 그 경우 per-core 합산을 cpu_count 로 나눠 같은 0–100% 스케일로 정규화.
- **XPU% (Windows)**: PDH `\GPU Engine(*)\Utilization Percentage` 를 PID 별 합산. 엔진(3D/Compute/Copy/…)별 합산이라 한 PID 가 100% 를 넘을 수 있음.
- **RSS**: `psutil` 로 자식 트리 합산.
- 두 PDH 카운터는 동일한 `_PdhWildcardCounter` 헬퍼를 공유 (instance name → key 함수만 다름).
- `Sampler` 스레드는 `threading.Thread` 의 내부 `_stop` 와 충돌하지 않게 `_stop_event` 라는 이름을 사용.

### 노트북 구성
두 개로 분리됨:
1. `bench/run_pipeline.ipynb` (preprocess + train 비교)
   - **10.1 preprocess**: `profile_preprocess.py --no-save` 를 numpy/rust 두 번 호출, 같은 분자 셋 비교.
   - **10.2 train**: `(loader, pad_impl)` 조합 4가지를 sampler로 측정.
   - **10.3 표 출력 + 10.4 시각화**: 각 그룹별 막대 + 시계열 + CPU+XPU 결합.
2. `bench/run_predict.ipynb` (predict 비교, 별도 파일)
   - 같은 sampler/시각화 인프라를 추론 경로(`bench/profile_predict.py`)에 한정해 실행.
   - 전처리·shard 산출물은 *재생성하지 않음* — `run_pipeline.ipynb` 가 만든 결과를 그대로 사용.
   - 시각화 결과는 `bench/figs/predict_{bars,timeseries,cpu_xpu}.png` 로 별도 저장.
- **10.4 시각화** (보고서용 dpi=160, `bench/figs/` 에 PNG 저장):
  - 그룹별 막대 4종(wall / peakRSS / meanCPU% / meanXPU%) + 값 라벨.
  - 그룹별 시계열 3패널(CPU% / RSS / XPU%) — 0.5 s window centered moving average 스무딩.
  - **(신규) 그룹별 CPU+XPU 결합 차트** — run 마다 twin y-axis (왼쪽 CPU%, 오른쪽 XPU%) + 영역 채움. 한 차트에서 호스트→XPU 바통터치가 시각적으로 보임.

### 검증 결과 (PDH CPU 도입 후, 소규모: 300 mol / 10 batches)
- preprocess[numpy]: 3.05 s, **CPU 80–100%**(단일코어 SciPy 풀가동), XPU 0%.
- preprocess[rust]:  1.09 s, **CPU 70–80%**(Rayon 8코어 + Python 파싱 혼합), XPU 0%.
- train[shard+python]: 14 s, CPU 60–100% (호스트 준비/IO), XPU% 가 t=8–13s 구간에서 60–120% 로 치솟음.
- 위 패턴이 결합 그래프에서 한눈에 보임 — 호스트가 먼저 일하다 XPU 가 받아 가는 형태.

### PDH 사용 시 주의
- `\Process V2(*)\% Processor Time` 는 시스템 전체 CPU 의 fraction(최대 100%). 옛 `\Process\` 카운터는 코어별 합산이라 N\*100 까지 가능 — 둘은 다른 컨벤션.
- 둘 다 priming 한 번 + 두 번째 collect 부터 유효한 값. 첫 번째 sample 값은 신뢰하지 않음.

## predict 경로 통합 (완료)
### `predict/predict.py` 토글 (기존 동작 보존)
- `--loader {npy,shard}` 추가. 기본 `npy` 는 기존 `train.MyDataset` 그대로.
  - `shard` 선택 시 `bench/convert_to_shard.py` 로 만든 `test_<field>_shard.bin` 을 mmap 으로 로드 (`train/dataset_shard.MyDatasetShard`).
- `--pad-impl {python,rust,rust-pad-only}` 추가. 기본 `python` 은 원본 `QuantumDeepField.pad / list_to_batch` 그대로.
  - `rust*` 선택 시 `train/model_patches.apply_rust_lcao` 가 인스턴스 메서드만 교체 (클래스 정의는 손대지 않음).
- 두 옵션 모두 기본값이 원본 동작과 1:1 이므로 기존 사용자에게 미치는 영향 없음.
- `predict/predict.sh` 에서는 `PREDICT_LOADER`, `PREDICT_PAD_IMPL` 환경변수로 동일 선택 가능.

### `bench/profile_predict.py` (신규)
- `profile_train.py` 의 추론 버전. backward / optimizer 없이 `model.forward(data)` (no_grad) 만 반복.
- 같은 토글(`--loader`, `--pad-impl`) + `--predict-mode {test,predict}` (test = `train.Tester.test` 와 동일 forward).
- 사전학습 가중치 불필요 (속도 측정 목적), 4가지 조합을 1코어 vs 8코어 vs XPU 부담 비교에 사용.
- 노트북 셀 28 (`PREDICT_COMPARE_RUNS`) 에서 NumPy vs Rust 조합으로 sampler 측정 → 시각화 셀이 자동으로 `predict` 그룹을 별도 figure 로 그림.

## `train.py` 본 스크립트 + 전처리 직접 shard (완료)
- `train/train.py`: `--loader {npy,shard}`(기본 `npy`), `--pad-impl {python,rust,rust-pad-only}`(기본 `python`). shard 선택 시 `train_<field>_shard.bin` 등이 없으면 안내 후 종료.
- `train/train.sh`: `TRAIN_LOADER`, `TRAIN_PAD_IMPL` 환경변수로 전달.
- `train/preprocess.py` / `predict/preprocess.py`: `--output-format {npy,shard,both}` 및 `create_dataset(..., output_format=)` — 전처리 중 `qdf_io.ShardWriter`로 단일 shard 기록(`.npy`와 병행 가능).
- `train/preprocess.sh`, `predict/preprocess.sh`: `PREPROCESS_OUTPUT_FORMAT`.

## 다음 후보 (남은 일)
1. ~~**`train/train.py` 본 스크립트에 `--loader` / `--pad-impl` 정식 도입**~~ **(완료)** — `train/train.py`에 `bench/profile_train.py`·`predict/predict.py`와 동일한 선택지 추가. `train/train.sh`는 `TRAIN_LOADER` / `TRAIN_PAD_IMPL`로 전달(기본 `npy` / `python`).
2. **Cold cache 벤치** — Windows에서 디스크 캐시 비우는 도구(RAMMap 등)로 IO/shard 격차 재검증.
3. ~~**Rust preprocess + shard writer 통합**~~ **(완료)** — `train/preprocess.py`의 `create_dataset(..., output_format=)`에 `npy` / `shard` / `both` 추가. Rust·NumPy 경로 모두 `qdf_io.ShardWriter`로 동일 레이아웃 기록 가능. CLI `--output-format`, `train/preprocess.sh`·`predict/preprocess.sh`의 `PREPROCESS_OUTPUT_FORMAT`으로 선택.
4. **LCAO rust 패치 재평가** — 다른 배치 크기/다른 GPU에서 유효성 재측정(현재 Arc 140V + batch 8에서는 비효율).

## shard writer Rust 이관 (완료)
- `qdf_io::ShardWriter` 추가: 헤더/인덱스/레코드 패킹·8-byte 정렬을 모두 Rust 가 처리하고 Python 은 ``np.load`` 만 수행. `__init__.py` 에 export.
- `train/dataset_shard.write_shard` 는 `qdf_io.ShardWriter` 가 import 되면 자동 사용하고, 안 되면 기존 순수 Python writer 로 폴백(원본 동작 보존).
- `bench/profile_preprocess.py` 의 임시 디렉터리는 `tempfile.TemporaryDirectory` 컨텍스트 매니저로 정리 — 별도 `shutil.rmtree` 호출/실제 데이터셋 경로 삭제 없음.
- 두 노트북(`run_pipeline.ipynb`, `run_predict.ipynb`)의 빌드 셀: 새 심볼(`ShardWriter`) 부재 시 자동 재빌드 시도, Windows 에서 `.pyd` 잠금으로 `maturin develop` 가 실패하면 `maturin build` 로 wheel 만 만들고 wheel 재설치까지 시도. 그래도 안 되면 커널 재시작 후 셀 재실행 안내.

## `qdf_io` Windows import / stale `.pyd` 대응 (에이전트 작업 요약, 2026-05)
### 증상
- `import qdf_io` 시 `ImportError: cannot import name 'ShardWriter' from 'qdf_io._native'` — **레포의 Rust 소스에는 `ShardWriter`가 있는데**, 옆에 깔린 **`_native*.pyd`만 예전 빌드**인 경우.
- Jupyter 등이 `.pyd`를 잡고 있으면 **`maturin develop --release`가 같은 경로로 덮어쓰기 실패**(Windows 오류 32).

### 코드 변경 (`QuantumDeepField_molecule/qdf_io/python/qdf_io/__init__.py`)
- 단일 `from ._native import …` 대신 **`_load_native()`** 로 모듈 결정. 순서:
  1. 기본 `._native` — **`ShardWriter` 속성이 있을 때만** 채택(구 `.pyd`는 여기서 탈락).
  2. **사이드카** `fresh_native_sidecar_path()` → 예: `_native.cp310-win_amd64.fresh.pyd` (수동으로 최신 DLL 복사해 두는 경로).
  3. 크레이트 트리 안 **`target/release/qdf_io.dll`** → 없으면 **`target/maturin/qdf_io.dll`** (`cargo build --release`만으로도 이 경로 갱신 가능, 보통 잠금 이슈 없음).
- 공개 헬퍼: `fresh_native_sidecar_path()`, `dev_native_dll_paths()`(튜플; `__all__`에 등록).
- **의도:** 커널/프로세스가 옛 `.pyd`를 잠가도, **소스 트리에 최신 DLL만 있으면** import가 통과하도록 함.

### 기타
- `train/preprocess.py`: `backend='rust'` 실패 시 `RuntimeError` 문구에 **`.pyd` 잠금 시 `cargo build --release` + `target/release/qdf_io.dll` 자동 로드** 안내 한 줄 추가.
- `qdf_io/.gitignore`: 로컬 빌드용 **`/.venv/`** 추가(로컬 `uv venv` 디렉터리 커밋 방지).

### 운영 팁
- 빠른 갱신: `cd QuantumDeepField_molecule\qdf_io` 후 **`cargo build --release`** (maturin 없이도 동작).
- 사이드카 수동 갱신(선택): `Copy-Item target\release\qdf_io.dll python\qdf_io\_native.cp310-win_amd64.fresh.pyd` (태그는 `python -c "import qdf_io; print(qdf_io.fresh_native_sidecar_path())"` 로 확인).

## Rust/벤치 통합 테스트용 노트북 (3DGCL / QDF)
- **`QuantumDeepField_molecule/bench/run_pipeline.ipynb`** — 전처리(numpy vs rust)·학습(loader/pad 조합) 등 **파이프라인 + sampler** 한 번에 돌리고 그림까지 (`bench/figs/`).
- **`QuantumDeepField_molecule/bench/run_predict.ipynb`** — 추론 경로(`profile_predict`)만, 전처리 산출물은 재생성하지 않고 파이프라인 결과 재사용.
- **`examples/sslgraph/bench/run_dgcl_rust.ipynb`** — DGCL(`dig_io`) 검증·벤치 통합 노트북. 환경 점검 → (선택) 빌드 → `verify_views_backend` → view fn 마이크로벤치(Python vs Rust) → `key_split` 정합성(size/set/disjoint) → `MoleculeNet`↔`MoleculeNetShard`(데이터셋 있을 때만) → `verify_dig_io_fallback` → 결과 표·막대그래프(`examples/sslgraph/bench/figs/bars_views_{ts}.png` 등). `dig_io` 미빌드/`.pt` 미존재 시 셀 단위로 자동 SKIP.
- 위 모든 노트북의 fig 저장은 실행 시각 `FIG_TS = strftime("%m%d_%H%M")` 를 파일명 끝에 붙임 (예: `bars_views_0512_1439.png`) — 재실행 시 기존 그림 덮어쓰지 않음.
- 그 외 `QuantumDeepField_molecule/tst.ipynb` 는 임시 스크래치 성격.

## DGCL 측 Rust 적용 (`dig_io/`, 완료)

### 신규 크레이트 `dig_io/`
- `QuantumDeepField_molecule/qdf_io/` 와 같은 PyO3 0.22 + numpy 0.22 + rayon + memmap2 패턴. `Cargo.toml`, `pyproject.toml`, `src/lib.rs`, `python/dig_io/__init__.py` (lazy 로더 + `target/release/dig_io.dll` 폴백, qdf_io와 동일).
- 빌드: `cd dig_io && c:\DGCL\3DGCL\.venv\Scripts\maturin.exe develop --release` (또는 `cargo build --release`).
- 노출 심볼: `uniform_sample_subgraph`, `rw_sample_subgraph`, `edge_perturb`, `scaffold_bucket_split`, `scaffold_bucket_sort`, `MoleculeShardWriter`, `MoleculeShardReader`, `format_info`, `is_available()`.

### 1. Contrastive view fn 토글
- `dig/sslgraph/method/contrastive/views_fn/sample.py` — `UniformSample(..., impl='python'|'rust')`, `RWSample(..., impl=...)`. 기본 `python` 으로 기존 동작 그대로. Rust 경로는 `dig_io` 가 있을 때만 사용.
- `dig/sslgraph/method/contrastive/views_fn/structure.py` — `EdgePerturbation(..., impl='python'|'rust')`. 원본 Python 의 `idx_remain` NameError 버그는 보존(노트로 남김), Rust 경로는 `add=True/drop=False` 케이스도 정상 동작.
- `examples/sslgraph/verify_views_backend.py` — Rust 결과의 결정성, 인덱스 정렬·subgraph relabel 정합성 검증(같은 seed 반복, NumPy reference relabel 일치).

### 2. MoleculeNet shard (`dig_io::MoleculeShardWriter/Reader`)
- 포맷 v1: `magic="DIGSHRD\0"` + 64B 헤더 + `u64` 인덱스 + 8B 정렬 레코드(`z,pos,edge_index,edge_attr,x,y,smiles, max1..4pos_mmff, min/max1..4_energy`). 자세한 레이아웃은 `dig_io/src/lib.rs` 상단 주석.
- `dig/threedgraph/dataset/MoleculeNetShard.py`:
  - `MoleculeNetShard(root, name)` — `{root}/{name}/processed/data.shard` mmap, PyG `Data` 로 복원. `int/list/slice/tensor` 인덱싱 모두 PyG `InMemoryDataset` 호환(`Subset` 반환).
  - `convert_inmemory_to_shard(src_dataset, dst_path)` — 기존 `MoleculeNet` 등 in-memory 데이터셋을 한 번 순회해 shard 생성.
  - `default_shard_path(root, name)`.
- `examples/sslgraph/convert_dataset_to_shard.py` — CLI. 기존 `data.pt` 는 손대지 않음.
- `dig.threedgraph.dataset.__init__` 에 `MoleculeNetShard`, `convert_inmemory_to_shard`, `default_shard_path` export.
- `dig/sslgraph/evaluation/finetune.py` 에 `args.loader ∈ {pt,shard}` 처리. 기본 `pt`. shard 미존재 시 자동으로 `pt` 폴백 + 안내.

### 3. `scaffold_split` / `key_split` Rust 가속
- `dig/threedgraph/dataset/dataset.py` 의 `key_split(dataset, keys, lengths, impl='python')`, `scaffold_split(dataset, seed, impl='python')` 에 `impl` 인자 추가.
- `impl='rust'` 는 **key permutation 은 torch 로** 유지(같은 RNG 시드면 결과 동일)하고, 그 뒤의 stable sort + key-boundary 라운딩만 `dig_io.scaffold_bucket_sort` 로 위임 → **Python 경로와 bit-for-bit 동일**.
- 수동 검증: 동일 keys/`torch.manual_seed(42)`에서 `key_split(impl='python')` vs `impl='rust'` 결과가 인덱스/오프셋까지 완전 일치.

### 4. 벤치
- `examples/sslgraph/bench/profile_views.py` — 합성 배치(`Batch.from_data_list`) 만들어 view 3종을 Python vs Rust 로 시간 측정.
- `examples/sslgraph/bench/profile_dataset_build.py` — `MoleculeNet`(pt) vs `MoleculeNetShard` 의 로드/이터레이션 비교(전처리된 데이터셋 필요).

### 1차 측정 (CPU 한 코어, 합성 배치 `n_graphs=128, n_nodes=30, n_edges=150`)
| kernel | python ms/call | rust ms/call | ratio |
|---|---:|---:|---:|
| `UniformSample(gin)` | 16.72 | 9.51 | **1.76x** |
| `RWSample(ratio=0.5)` | 57.28 | 25.48 | **2.25x** |
| `EdgePerturbation(add+drop)` | 59.91 | 9.01 | **6.65x** |

- EdgePerturbation 의 dedup/`torch.unique(dim=1)` 가 Python 측에서 가장 비싼 부분이라 격차가 가장 큼.
- RWSample 은 Rust 가 `HashSet` 기반 frontier 를 정상 확장하지만 `data.edge_index.cpu().numpy()` 변환이 분자별로 한 번씩 들어 가서 격차가 좁음. batch 사이즈가 커질수록 격차 확대 예상.

### 가드레일 (DGCL 측)
- `dig_io` 미빌드 환경에서 `import dig_io` 는 **성공** (모든 심볼이 ImportError 를 던지는 stub 으로 채워짐) + `is_available()` 가 False.
- views_fn/dataset/scaffold split 모두 `impl='python'` 또는 `loader='pt'` 가 기본이라 기존 사용자 코드는 영향 없음.
- 검증 스크립트: `examples/sslgraph/verify_views_backend.py`, `key_split` 동치 확인(노트북 셀로도 재현 가능).

### 적용/미적용 정리
- 적용: contrastive view fn, MoleculeNet 로더, scaffold split bucket 산술.
- 미적용(의도): SchNet/DimeNet/SphereNet `forward`, `xyz_to_dat` 등 autograd 그래프 안의 GPU 코드, SymPy 기반 `features.py`(한 번만 도는 init 비용).

## Pretrain 속도 (Rust 검토 → 결국 torch 벡터화로 해결, 2026-05)

### 측정
`examples/sslgraph/bench/profile_pretrain.py` 로 한 epoch 을 단계별로 쪼개 측정 (ESOL/batch=400/XPU).

| 구성 | wall | dataloader | to_dev | views_fn | fwd+loss | backward | step |
|---|---:|---:|---:|---:|---:|---:|---:|
| baseline | 6106 ms | 149 | 19 | **3686** | 1476 | 765 | 10 |
| + `MMFFRANDOM_FAST=1` | 2972 ms | 144 | 19 | 11 | 1967 | 818 | 12 |
| + `PRETRAIN_AMP=bf16` | **2070 ms** | 96 | 14 | 9 | 1040 | 903 | 7 |

병목은 GPU compute 가 아니라 PyG `to_data_list/from_data_list` (60%). views 한 번에 ~752회 Data 객체 재구성. → Rust 대신 단일 `torch.index_select` 로 350× 절감.

### 변경
- `dig/sslgraph/method/contrastive/views_fn/mmff_fast.py` 신규: `FastRandomMMFFView(slots)` — 배치된 `max{1..4}pos_mmff`/`max{1..4}_energy` 슬랩에서 그래프별 uniform pick + `index_select` 한 번으로 새 `Batch` 생성. PyG list 왕복 0회.
- `dig/sslgraph/method/contrastive/model/graphcl.py` — `aug='MMFFrandom'` 시 `args.mmffrandom_impl='fast'` 또는 env `MMFFRANDOM_FAST=1` 일 때 fast path. 기본은 기존 `RandomView` 그대로.
- `dig/sslgraph/method/contrastive/model/contrastive.py` — `epoch_loss += loss` 가 autograd 그래프를 epoch 끝까지 유지하던 문제를 `loss.detach()` 로 수정.
- `dig/sslgraph/utils/dataloader_kw.py` — `PIN_MEMORY=1` 환경변수 추가(H2D 복사 가속).
- `examples/sslgraph/pretrain.ipynb` 셀에 위 환경변수 설명만 추가 (default 켜지 않음).

### 가드레일
- fast path 출력 분포는 원본과 통계적으로 동일 (2개 슬롯 uniform). 단일 슬롯의 경우 NodeTranslation 결과와 비트단위 일치.
- 미선택 시 기존 `RandomView`/`NodeTranslation` 경로 100% 그대로 — 사용자 노트북 영향 없음.

### 비파괴 A/B 시각화 (QDF 스타일)
- `examples/sslgraph/bench/compare_pretrain_ab.py` — 호스트 `os.environ` 은 **스냅샷만 출력**; 실제 override 는 `pretrain_bench_core.isolated_env()` 컨텍스트 안에서만 적용 후 원복. Windows 에서 QDF `QuantumDeepField_molecule/bench/sampler.py` 를 `sys.modules` 에 선등록 후 동적 로드(PDH `Process V2` + `GPU Engine`). 출력: `figs/pretrain_ab_<ts>/` 에 `pretrain_ab_results.json`, `bars_pretrain_ab_*.png`, `bars_pretrain_stages_*.png`, `timeseries_cpu_xpu_*.png`.
- `examples/sslgraph/bench/run_pretrain_compare.ipynb` — 동일 스크립트 실행 + 이미지 표시.

### Rust 결론
- DGCL pretrain 의 실제 병목은 PyG `Batch` 분해/재조립으로, NumPy/Rust 왕복비용을 감안하면 Rust 로 옮길 ROI 가 사실상 0. **하지 않음.**
- Contrastive view fn (Uniform/RW/EdgePerturbation) Rust 경로(`dig_io`)는 여전히 유효 — fast-MMFF 와 보완 관계.

## MMFF 슬롯 가중 선택 (랜덤 → QDF/Boltzmann, 2026-05)

### 동기
- 기존 `MMFFrandom`: 학습 시작 시 4 슬롯 중 2개 `random.sample` → 그래프마다 균등(50:50). 안정성 정보 미사용.
- 목표: 같은 분자라도 **존재 확률(=안정성)** 이 높은 conformer 슬롯에 더 자주 가중.
  - Slot-level 가중치 `w[g, 1..4]` → view 가 `multinomial` 로 그래프별 categorical pick.

### 추가 부품
- `dig/sslgraph/method/contrastive/views_fn/mmff_fast.py`
  - `WeightedMMFFView(slots, kT, weight_mode, weight_norm)` — 4 슬롯 categorical (uniform 도 가능). `FastRandomMMFFView` 와 동일한 vectorised slab `index_select` 백본.
  - Weight 우선순위: `batch.mmff_weights[g, K]` → 없으면 Boltzmann `softmax(-(E-Emin)/kT)` of `maxK_energy`. `weight_mode='auto'|'explicit'|'boltzmann'`, `weight_norm='softmax'|'linear'`.
- `dig/sslgraph/method/contrastive/views_fn/mmff_weights_io.py`
  - `load_weights(path) -> {smiles: tensor[K]}` (SMILES strip 정규화 포함, MoleculeNet 의 trailing-whitespace 분자 호환).
  - `apply_mmff_weights(dataset, weights, default=None)` — InMemoryDataset 의 `_data` + `slices['mmff_weights'] = arange(N+1)` 정식 경로로 collate. 결측 분자는 uniform 행으로 채워 explicit 모드도 안전.
- `dig/sslgraph/method/contrastive/model/graphcl.py`
  - 새 augmentation `aug = 'MMFFweighted'` 분기 + 기존 `aug = 'MMFFrandom'` 의 `args.mmffrandom_impl='weighted'` 라우팅. env `MMFFRANDOM_WEIGHTED=1` 도 인식. 기본은 'python' 그대로.

### 가중치 빌드 도구
- `examples/sslgraph/bench/compute_mmff_weights.py`
  - `--source boltzmann` — 데이터셋의 `maxK_energy` 만으로 즉시. 별도 모델 불필요.
  - `--source qdf --pred-csv` — **QDF homolumo 체크포인트** 출력 CSV (`smiles, slot, homo, lumo`) 를 `--score-expr` 점수 → softmax. 기본 점수 `-(lumo-homo)` 는 HOMO-LUMO gap 클수록(안정) 가중치 ↑.
  - 출력: `dataset/<name>_mmff_weights_<source>.pt` (`qdf_property: homolumo` 메타). atomization-energy 모델은 사용하지 않음(정책).
- `examples/sslgraph/bench/qdf_mmff_predict.py`
  - DGCL MoleculeNet 의 분자 × MMFF 슬롯 1..4 좌표 + `data.z` 로 QDF 입력(record 6-tuple) 직접 구성 → `QDF(predict=True)` 로 (homo, lumo) 추론 → CSV.
  - QDF 사전학습 시 orbital_dict (`QM9under7atoms_homolumo_eV/orbitaldict_6-31G.pickle`) 미등록 원소(P/S/Cl/Br/I 등) 자동 스킵 → 다운스트림 Boltzmann fallback 으로 메움.
  - ESOL 1128 분자 × 4 슬롯 = 4512 시도 → XPU(Arc 140V) 90초, batch=16. 매칭 SMILES 735개(미등록 393개 제외).

### A/B/C 시각화 확장
- `examples/sslgraph/bench/compare_pretrain_ab.py` 에 optional `--c-label/--c-set/--c-clear` 추가. JSON `summary_c`, `run_c` 와 막대/시계열 3-column 까지 지원.
- `pretrain_bench_core.run_pretrain_benchmark` 에 env hook `MMFF_WEIGHTS_PATH` 추가: 값이 있으면 dataset 로드 직후 `apply_mmff_weights` 자동 호출(`isolated_env` 안에서만). 결과 메타에 `mmff_weights` 필드(attached/missing/path).
- `examples/sslgraph/bench/run_pretrain_compare.ipynb` 의 cell 1 에 `COMPARE_MODE = "abc"` + `WEIGHT_SOURCE in {"boltzmann","qdf"}` 토글 추가. 가중치 `.pt` 없으면 QDF 모드는 `qdf_mmff_predict.py → compute_mmff_weights.py` 체인을 자동 호출.

### 측정 (ESOL, batch 400, warmup 0, 1 epoch × 2 iters)
| 구성 | wall | mean_cpu | mean_xpu | peak_rss |
|---|---:|---:|---:|---:|
| A baseline (PyG views) | 5.53 s | 86.4% | 37.9% | 5187 MB |
| B optimized (FAST + bf16) | 1.74 s | 96.8% | 53.4% | 5707 MB |
| C weighted (QDF gap, bf16) | 1.68 s | 96.3% | 48.9% | 6153 MB |

C ≈ B (가중치 lookup 1회 추가지만 동일 vectorised path). 정확도 효과 검증은 별도 finetune 비교 권장.

### 가드레일
- 기본 동작 그대로(env 미설정 시 `python` impl). `WeightedMMFFView` 미선택 시 코드 영향 없음.
- 가중치 `.pt` 미존재 시 자동 빌드 (boltzmann 즉시 / qdf 는 체크포인트 필요).
- ESOL 의 SMILES 일부에 trailing space 가 있는 데이터 정합성 문제는 `load_weights` / `apply_mmff_weights` / `compute_mmff_weights` 양쪽에서 `strip()` 적용으로 해결.

## 품질(RMSE) 비교 노트북 + 슬롯 분포 검증 (2026-05)

### 동기
- `compare_pretrain_ab.py` 는 *속도/리소스* 비교 전용. \"weighted 가 정말 모델 성능을 올리는가\" 는 별도 다운스트림 finetune 으로만 측정 가능.
- 동시에 \"진짜 랜덤이 weighted 로 바뀌었는가\" — view 함수 호출 분포 검증도 필요.

### 추가 부품
- `examples/sslgraph/bench/pretrain_quality_core.py`
  - `run_pretrain_side(side, dataset, batch_size, p_epoch, model_root, device, weights_path)` — 측면별로 별도 `model_root` 에 GraphCL 풀-프리트레인 (A=baseline / B=fast / C=weighted). 끝나면 `Contrastive.last_epoch_losses` 를 통해 epoch별 loss 리스트, 최신 `enc_best_*.pkl` 경로 반환.
  - `run_finetune_side(pre_summary, dataset, batch_size, f_epoch, n_times, n_folds, device)` — 측면 체크포인트로 `Finetune.evaluate()` 직접 호출 (단일 hyperparam 이라 `grid_search` 의 Windows path 파싱 우회). RMSE 평균±SD 반환.
  - `measure_slot_distribution(side, dataset, batch_size, n_calls, device, weights_path)` — 한 배치에 view 함수 N 회 호출, 결과 `out.pos` 와 `batch.max{1..4}pos_mmff` slab 정확 비교 (`atol=1e-5`) 로 슬롯 라벨 검출. 슬롯 빈도, KL(observed‖uniform), per-graph entropy 반환.
  - `_clean_aug_env()` 컨텍스트 매니저: `MMFFRANDOM_FAST/WEIGHTED`, `MMFF_WEIGHTS_PATH` 를 측면 실행 동안만 unset (host env 비파괴).
- `dig/sslgraph/method/contrastive/model/contrastive.py` 에 한 줄 추가: `self.last_epoch_losses = [float(l) for l in losses]` — 외부 runner 가 loss curve 캡처할 수 있게.
- `examples/sslgraph/bench/compare_pretrain_quality.ipynb`
  - Cell 1 setup: `P_EPOCH` / `F_EPOCH` / `BATCH_SIZE` / `WEIGHT_KT` / `WEIGHT_SOURCE` 등 노브.
  - Cell 3 weights ensure: 기존 `dataset/esol_mmff_weights_qdf.pt` 가 없으면 `qdf_mmff_predict.py → compute_mmff_weights.py` 체인 자동 호출.
  - Cell 4 slot dist plot (3 sub-axes, 4 bars each, `bar_label`, KL/entropy 표기) → `figs/pretrain_quality_<ts>/slot_distribution_*.png`.
  - Cell 5/6 pretrain A/B/C + loss curve plot.
  - Cell 7/8 finetune A/B/C + RMSE 막대 (yerr=SD, 값 라벨 박힘).
  - Cell 9 summary CSV (`summary.csv`) 와 표.

### 슬롯 분포 검증 결과 (ESOL, batch 256, 10 calls)
| 측면 | view class | n_slots_used | KL(uniform) [bits] | per-graph H̄ [bits] |
|---|---|:-:|---:|---:|
| A baseline (`RandomView`)         | `RandomView`         | **2** | 1.001 | 0.79 |
| B fast (`FastRandomMMFFView`)     | `FastRandomMMFFView` | **2** | 1.001 | 0.87 |
| C weighted (`WeightedMMFFView`)   | `WeightedMMFFView`   | **4** | 0.009 | 1.77 |

A/B 는 미리 sample 한 2 슬롯만 (KL ≈ log2 4 / 2 = 1 bit, half-mass), C 는 4 슬롯 전부 사용해 분포가 거의 uniform 에 가까움 (kT=1 + QDF gap 이 분자별로 비슷한 값을 주기 때문). kT 를 줄이면 분포가 첨예해짐.

### 가드레일
- 노트북의 `MODEL_ROOT = ./models/quality_<FIG_TS>/` 에 측면별 폴더 분리 — `enc_best_*.pkl` 충돌 없음.
- `Pretrain.evaluate` / `Finetune.evaluate` 둘 다 측면 실행 동안 `_clean_aug_env()` 로 둘러싸 host 의 MMFFRANDOM_* env 와 무관하게 explicit `args` 만 작용.
- 스모크 검증: side A, P_EPOCH=1, F_EPOCH=2, batch=256, 3-fold → 24 초, RMSE 1.68±0.21 (pretrain 5 s, finetune 19 s) — pipeline 정상.

### 두 가지 가중치 픽업 버그 수정 (2026-05)

이전 가중치 빌드 결과가 거의 uniform (~0.25) 으로만 나오는 이유 두 가지를 찾아 수정:

**(1) QDF 예측 spread 가 슬롯 간 너무 작아 softmax 가 평탄해짐.**
같은 분자의 MMFF 1..4 rotamer 들에 대해 QDF (QM9 ground-state 좌표로 학습) 가 거의 같은 HOMO/LUMO 를 예측: median spread 0.0012 eV, p99 0.007 eV, 반면 분자 간 gap range 는 2.44 eV. 비율 ~5×10⁻⁴.

→ `compute_mmff_weights.py qdf_weights` 에 `--normalize {none,zscore,rank}` 추가 (기본 `zscore`).
- `zscore`: 각 분자의 4 슬롯 점수를 (mean=0, std=1) 로 표준화 후 softmax. 절대 magnitude 와 무관하게 \"상대 ranking\" 만 살림. 결과: top-slot 확률 p50=0.557, p90=0.733 (was 모두 ~0.25).
- `rank`: 점수→0..K-1 rank 로 대체 (deterministic 분포).

**(2) `WeightedMMFFView` 가 이미 softmax 된 확률에 또 softmax 를 거는 이중 softmax.**
`compute_mmff_weights.py` 가 저장하는 `.pt` 의 rows 는 이미 합=1 인 확률. 하지만 view 의 `weight_norm='softmax'` (구 기본) 가 한 번 더 softmax → 피크 [0.07,0.77,0.07,0.07] 가 [0.20,0.39,0.20,0.20] 으로 평탄화.

→ `WeightedMMFFView.weight_norm` 에 `'auto'` 추가 + 기본값을 `'auto'` 로 변경.
- 각 row 가 `≥0` 이고 `sum ≈ 1±1e-3` 이면 확률로 간주해 그대로 통과; 그렇지 않으면 logits 로 보고 softmax.
- 호출 측 기본값도 정렬: `graphcl.py` 의 `getattr(args,'mmff_weight_norm','softmax')` → `'auto'`; `pretrain_quality_core.configure_side(weight_norm='softmax')` → `'auto'`.

### 분포 검증 (수정 후, ESOL batch=128, 40 calls)
| 측면 | view | slots_used | KL(uniform) | **per-graph H̄** |
|---|---|:-:|---:|---:|
| A baseline | `RandomView` | 2 | 1.000 b | 0.980 b |
| B fast | `FastRandomMMFFView` | 2 | 1.000 b | 0.981 b |
| **C weighted (FIXED)** | `WeightedMMFFView` | 4 | 0.000 b | **1.643 b** ← was 1.966 b (사실상 uniform) |

ideal 기대값 1.481 b 와 1.643 b 의 차이는 QDF 예측이 없는 ESOL 393/1128 (35%) 분자가 uniform [0.25]*4 fallback 으로 채워지기 때문: 0.65×1.48 + 0.35×2.00 ≈ 1.66. 일치.

분자별 CSV 확인 (`dataset/esol_mmff_weights_qdf.csv`, `top_minus_uniform` 정렬):
- 최상위: top slot 77% / 나머지 ~7% (Cc1cccnc1C, MMFF2 가 top)
- 731 분자 전부 top_w ≥ 0.42, p50=0.557, p99=0.770.

### atomization-energy QDF 로 전환 (2026-05)

HOMO/LUMO 만으로는 z-score normalize 후에도 conformer 차이가 작아(원래 spread 가 ~1 meV, 분자 간 절대값보다 5×10⁻⁴ 작음) 정보량이 부족했음. atomization energy 는 conformer 의 potential-well 깊이를 직접 보므로 훨씬 민감 (spread ~0.3 eV, **100–500×** 더 큼).

**구현 변경**
- `examples/sslgraph/bench/qdf_mmff_predict.py` 에 `--qdf-property {homolumo,atomization}` 추가.
  - 각 property 별 preset (`_PROPERTY_PRESETS`): `qdf_trained` 데이터셋, `dim`, `hidden_HK`, `layer_HK`, `operation`, `N_output`, 기본 checkpoint, CSV header.
  - `atomization` preset: `QM9under14atoms_atomizationenergy_eV`, dim=250, op=sum, N_out=1, ckpt=`output/model--QM9under14atoms_atomizationenergy_eV--…--dim250--hidden_HK250--sum--batch_size4--iteration2000`.
  - `homolumo` preset (legacy): `QM9under7atoms_homolumo_eV`, dim=200, op=mean, N_out=2, ckpt=`output/model--QM9under7atoms_homolumo_eV--…--dim200--hidden_HK200--mean--batch_size8--iteration2000`.
  - CSV 출력 파일명: `<name>_qdf_mmff_preds_<property>.csv`.
- `examples/sslgraph/bench/compute_mmff_weights.py`
  - `QDF_CSV_SCHEMAS` 로 CSV 헤더 컬럼에서 schema 자동 감지 (`{homo,lumo}` ⇒ homolumo / `{energy}` ⇒ atomization).
  - `_safe_eval` 일반화: schema 의 `score_namespace` 만 허용.
  - `qdf_weights()` 가 `(weights, property, score_expr_used)` 튜플 반환 — payload 메타에 동적으로 기록.
  - atomization 기본 score: `-energy` (QDF 출력은 부호 음수, 더 음수 = 더 안정 ⇒ softmax 대상 점수는 양수가 되어야 하므로 부호 반전).
  - homolumo 기본 score 는 그대로 `-(lumo - homo)`.
- `examples/sslgraph/bench/dump_mmff_weights.py` 헤더에 `score_expr`, `normalize` 추가 출력.
- `examples/sslgraph/bench/compare_pretrain_quality.ipynb`
  - cell 1: `QDF_PROPERTY = 'atomization'` (기본), `QDF_CHECKPOINT = None` (preset), `SCORE_EXPR = None` (preset), `WEIGHT_NORM = 'zscore'`.
  - cell 1: 가중치/CSV 파일명에 property 접미 (`esol_mmff_weights_qdf_atomization.pt`).
  - cell 3 (weights ensure): `--qdf-property` / `--score-expr` / `--normalize` / 선택적 `--checkpoint` 를 자식 프로세스에 forward; 마지막에 `torch.load` 해서 메타 즉시 확인.

**smoke (ESOL 처음 10 분자, atomization)**
- per-slot energy spread 예시: `OCC3OC(...) → [-254.29, -254.01, -254.26, -254.54]` (0.5 eV), `CCCC=C → [-59.14, -59.46, -59.38, -59.21]` (0.3 eV), `CC12...(스테로이드) → [-192.56, -192.76, -192.88, -192.73]` (0.3 eV).
- z-score 정규화 후 weights: top slot p≈0.55–0.76, top slot 이 분자마다 다양 (2/3/4 모두 등장), entropy 1.17–1.51 b (uniform 2.0 b).
- 즉 conformer 별 우호도가 **확실히** 다르게 잡힘.

**가드레일**
- 캐시 파일명에 property 가 포함되므로 homolumo 결과(`esol_mmff_weights_qdf.pt`)와 atomization 결과(`esol_mmff_weights_qdf_atomization.pt`)는 자동 분리 — 한쪽이 다른 쪽을 오염시키지 않음.
- 다른 QDF ckpt 시도 시: `QDF_CHECKPOINT='QuantumDeepField_molecule/output/model--…'` 로 노트북에서 1줄 변경. 단, dim/hidden_HK/op 가 다른 ckpt 면 `qdf_mmff_predict.py --dim --hidden-hk --layer-hk --operation` 도 명시 필요 (preset 은 property 단위라 architecture 변경은 수동).
- QDF 가 모르는 원소(P/S/Cl/Br/I 등) 가 들어 있는 분자는 CSV 에 안 들어가고, downstream `WeightedMMFFView(weight_mode='auto')` 가 MMFF energy Boltzmann fallback 으로 채움 — atomization 으로 바꿔도 동일 정책 유지.

### QDF / 서버 이전 / 학습 파이프라인 (2026-05-14)

**리눅스 이전·압축**
- `C:\DGCL\3DGCL_for_linux.zip` 등으로 옮길 때: 소스 + `dig_io`/`qdf_io` Rust 소스, ESOL 산출물, `QM9full_homolumo_eV`의 `train/val/test.txt` + `orbitaldict`, QDF `output/` 체크포인트 등. 대용량 `*_shard.bin`/npy 폴더는 선택(서버에서 재전처리 가능).
- `train.sh` 주석: `preprocess.py` 인자는 **`--dataset` 없음** — 위치 인자 4개 `dataset basis_set radius grid_interval` 후 `--backend rust --output-format both`.

**전처리**
- `preprocess.py`는 **GPU/XPU 미사용**; NumPy/SciPy 또는 `--backend rust`(Rayon CPU).
- `QM9full_homolumo_eV` 전처리 완료 시 `train/val/test_6-31G_0.75sphere_0.3grid_shard.bin` 생성 확인.

**`train/train.sh` (QM9full homolumo 기본)**
- `dataset=QM9full_homolumo_eV`, `iteration=2000`, `TRAIN_LOADER=shard`, `TRAIN_PAD_IMPL=rust`, `num_workers=4`.
- 기본 `MAX_TRAIN_SECONDS=3600`(1 h); 비우면 무제한: `MAX_TRAIN_SECONDS= bash train.sh`. 시간만 2배는 `7200` 등으로 조정.
- `EARLY_STOP_PATIENCE`: unset 시 스크립트 기본 30; 끄려면 `EARLY_STOP_PATIENCE= bash train.sh`.

**`train/train.py`**
- 조기 종료: `--max-train-seconds`, `--early-stop-patience`, `--early-stop-min-delta`.
- 학습 **벽시간 표시**: epoch마다 `this_epoch` / `cumulative` 한 줄; 종료 시 총 시간·epoch 수·epoch당 평균; `result--*.txt` 맨 아래 `# total_wall_sec=...` 요약 줄.

**Windows + shard + `num_workers>0`**
- 증상: `TypeError: cannot pickle 'qdf_io._native.ShardReader'` 및 워커 `EOFError: Ran out of input`.
- 원인: `DataLoader` spawn 시 `MyDatasetShard` 전체 pickle → Rust `ShardReader` 불가.
- 수정: `train/dataset_shard.py`의 `MyDatasetShard`에 `__getstate__`/`__setstate__`로 **`shard_path`만** 직렬화, 워커에서 `ShardReader` 재오픈.

**실행 상태 확인 (에이전트 스냅샷 기준)**
- 터널 로그: pickle 수정 **이전** `train.sh` 실행은 위 TypeError로 실패. 수정 **이후** 재실행은 `Wall-clock budget: 7200`까지 출력된 뒤 **epoch 0 한 줄이 아직 안 보임** — full 첫 epoch이 매우 길면 정상적으로 한동안 출력이 없을 수 있음. 진행 여부는 (1) 터미널에 `0\t<Time>` 첫 줄이 뜨는지 (2) `QuantumDeepField_molecule/output/result--QM9full_homolumo_eV--...txt` append 여부 (3) CPU/GPU 사용률 로 확인.
- 로컬 디바이스: 로그상 **Intel XPU** 사용.

**남은 작업 (학습 끝난 뒤)**
- 새 ckpt로 ESOL 파이프라인 쓰려면: `qdf_mmff_predict.py` preset/인자 → `compute_mmff_weights.py` → 노트북 `QDF_CHECKPOINT` 정렬.

### 벤치 UX·병렬 튜닝 노트 (2026-05-14)

**QDF 앙상블 오케스트레이터 — 노트북에서 “멈춘 것처럼” 보이던 문제**
- 대상: `examples/sslgraph/bench/ensemble_cpu_xpu_orchestrator.py`.
- 원인: `_run()`이 자식 프로세스 stdout/stderr를 **로그 파일로만** 리다이렉트해서, Jupyter에서 `subprocess.run(...)`으로 호출하면 셀 출력이 긴 구간 동안 비어 있음.
- 변경: `subprocess.Popen` + 라인 단위 읽기로 **로그 파일에 쓰면서 동일 내용을 `print(..., flush=True)`** 로 흘림(tee). 단계 시작/종료(`OK`/`FAILED`·wall), `run_mode`별 `# MODE: CPU|XPU`, `main`에서 run_root·XPU 스킵 사유·종료 한 줄 요약 출력.

**`rust_parallel_tuning_report.ipynb` — Rust vs NumPy 병렬 스윕·시각화**
- §6: NumPy 백엔드용 **`OMP_NUM_THREADS` / `OPENBLAS_NUM_THREADS` / `MKL_NUM_THREADS` / `NUMEXPR_NUM_THREADS`** 를 동일 값으로 묶어 스윕(`bench_numpy`), 출력 키는 `BLAS_NUM_THREADS`로 표기(의미상 BLAS 스레드 상한).
- §7: 한 figure에 (좌상) 스레드 vs 절대 wall, (우상) 1-thread 대비 speedup, (하) best-of 바 + `NumPy / Rust` 배속 주석; 저장 `examples/sslgraph/bench/figs/parallel_scaling_rust_vs_numpy.png`.
- §8: 위 3패널을 **개별 PNG**로도 저장 — `figs/scale_wall.png`, `figs/scale_speedup.png`, `figs/scale_bestof.png`.
- §9: `psutil`(venv에 설치됨)으로 서브프로세스를 **0.05 s 간격** 샘플링하는 `bench_with_usage()` — Rust·NumPy 각각 **앞 스윕에서 고른 최적 스레드 수**로 `profile_preprocess.py` 1회씩 실행, 시계열로 **CPU% · RSS(MB) · OS 스레드 수** 겹쳐 그림; 저장 `figs/usage_timeseries.png`. 점선은 해당 실행의 wall 종료 시각.

**해석 메모 (`RAYON_NUM_THREADS` vs “CPU 스레드”)**
- `RAYON_NUM_THREADS`는 Rust Rayon 풀의 **논리 CPU 스레드 상한**과 동일하게 이해하면 됨. NumPy 경로는 분자 루프가 순차라 BLAS 스레드만 올려서는 이득이 제한적일 수 있음 — 의미 있는 곡선은 `SMOKE=False` 등으로 `PRE_LIMIT`를 키운 뒤 재측정 권장.
